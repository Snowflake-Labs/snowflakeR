% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r_executor.R
\name{sfr_log_executor}
\alias{sfr_log_executor}
\title{Register a Generic R Executor in Model Registry}
\usage{
sfr_log_executor(
  reg,
  model_name,
  version_name = NULL,
  r_script_path,
  partition_columns = "SKU",
  r_packages = character(0),
  entry_fn = "main",
  diagnostic_mode = FALSE,
  input_columns = NULL,
  output_columns = NULL,
  conda_deps = NULL,
  pip_deps = NULL,
  comment = NULL
)
}
\arguments{
\item{reg}{An \code{sfr_model_registry} or \code{sfr_connection} object.}

\item{model_name}{Character. Model name in the registry.}

\item{version_name}{Character. Version label. If \code{NULL}, auto-generated
from timestamp.}

\item{r_script_path}{Character. Path to the R script on local disk.
Must define the entry function.}

\item{partition_columns}{Character vector. Column names for partitioning
(default \code{"SKU"}).}

\item{r_packages}{Character vector. R packages to load before sourcing
the script (default empty).}

\item{entry_fn}{Character. Name of the R function to call per partition
(default \code{"main"}).}

\item{diagnostic_mode}{Logical. If \code{TRUE}, the executor returns only
diagnostic columns (status, timing, error) instead of the R function's
result. Useful for long-running tasks that write results elsewhere.}

\item{input_columns}{Named list of column definitions. Each element is
\code{list(name = "COL", type = "STRING")}. If \code{NULL}, partition columns
are used as STRING inputs.}

\item{output_columns}{Named list of output column definitions. If \code{NULL},
diagnostic schema is used.}

\item{conda_deps}{Character vector. Additional conda dependencies.}

\item{pip_deps}{Character vector. Additional pip dependencies.}

\item{comment}{Character. Optional comment.}
}
\value{
A list with \code{model_name}, \code{version_name}, \code{registration_time_s}.
}
\description{
Registers an R script as a \code{TABLE_FUNCTION} model version in Snowflake
Model Registry. The script must define an entry function that accepts
a data.frame and returns a data.frame. The function is called once per
partition.
}
\details{
The registered model can be invoked from SQL:

\if{html}{\out{<div class="sourceCode sql">}}\preformatted{SELECT * FROM TABLE(
  MY_EXECUTOR!execute(
    SELECT SKU, PARAMS FROM WORK_TABLE
  ) OVER (PARTITION BY SKU)
)
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
conn <- sfr_connect()

# Register a simple executor
result <- sfr_log_executor(
  conn,
  model_name = "SKU_OPTIMISER",
  r_script_path = "optimise_sku.R",
  partition_columns = "SKU",
  r_packages = c("dplyr", "nloptr"),
  entry_fn = "optimise",
  diagnostic_mode = TRUE
)

# Now callable from SQL:
# SELECT * FROM TABLE(SKU_OPTIMISER!execute(
#   SELECT SKU, PARAMS FROM WORK_MANIFEST
# ) OVER (PARTITION BY SKU))
}
}
