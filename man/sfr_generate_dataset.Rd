% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/features.R
\name{sfr_generate_dataset}
\alias{sfr_generate_dataset}
\title{Generate an immutable, versioned Dataset from Feature Views}
\usage{
sfr_generate_dataset(
  fs,
  name,
  spine,
  features,
  version = NULL,
  spine_timestamp_col = NULL,
  spine_label_cols = NULL,
  desc = ""
)
}
\arguments{
\item{fs}{An \code{sfr_feature_store} object.}

\item{name}{Dataset name.}

\item{spine}{A data.frame, SQL string, or dbplyr lazy table providing
entity keys and (optionally) labels.}

\item{features}{A list of registered \code{sfr_feature_view} objects or
lists with \code{name} and \code{version}.}

\item{version}{Dataset version string. When \code{NULL} (the default),
Snowflake auto-generates a version.}

\item{spine_timestamp_col}{Optional timestamp column for point-in-time
correctness.}

\item{spine_label_cols}{Optional character vector of label column names.}

\item{desc}{Optional description for the dataset.}
}
\value{
A data.frame of training data with attributes
\code{dataset_name} and \code{dataset_version} attached. Pass this object
to \code{\link[=sfr_log_model]{sfr_log_model()}} via \code{training_dataset} to complete
Feature View -> Dataset -> Model lineage.
}
\description{
Creates a Snowflake ML Dataset that participates in ML Lineage
(Feature View -> Dataset -> Model). The returned data.frame carries
\code{dataset_name} and \code{dataset_version} attributes that
\code{\link[=sfr_log_model]{sfr_log_model()}} uses to wire the lineage chain.
}
\examples{
\dontrun{
conn <- sfr_connect()
fs <- sfr_feature_store(conn)

ds <- sfr_generate_dataset(
  fs,
  name = "CHURN_TRAINING",
  spine = "SELECT customer_id, label FROM labels_table",
  features = list(
    list(name = "CUSTOMER_FEATURES", version = "v1")
  ),
  version = "v1",
  spine_label_cols = "label"
)

# Train a model using ds as a plain data.frame ...
# Then log with lineage:
sfr_log_model(conn, model, "CHURN_MODEL",
              training_dataset = ds)
}

}
\seealso{
\code{\link[=sfr_generate_training_data]{sfr_generate_training_data()}}, \code{\link[=sfr_log_model]{sfr_log_model()}}
}
