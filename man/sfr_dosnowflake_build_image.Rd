% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dosnowflake_setup.R
\name{sfr_dosnowflake_build_image}
\alias{sfr_dosnowflake_build_image}
\title{Build and push a doSnowflake worker Docker image}
\usage{
sfr_dosnowflake_build_image(
  conn,
  image_repo,
  tag = "latest",
  variant = c("lean", "full"),
  packages = NULL,
  docker_context = NULL
)
}
\arguments{
\item{conn}{An \code{sfr_connection} object from \code{\link[=sfr_connect]{sfr_connect()}}.}

\item{image_repo}{Character. Name of the Snowflake image repository
(e.g. \code{"MY_REPO"}).}

\item{tag}{Character. Image tag (default \code{"latest"}).}

\item{variant}{Character. \code{"lean"} (R only, ~500 MB) or
\code{"full"} (R + Python + reticulate, ~2 GB).}

\item{packages}{Named list of additional packages to install:
\itemize{
\item \code{conda} -- character vector of conda-forge package names
\item \code{cran} -- character vector of CRAN package names
\item \code{pip} -- character vector of pip package names
}}

\item{docker_context}{Character. Path to the directory containing the
Dockerfiles and worker.R. If \code{NULL}, uses the built-in templates
from \verb{internal/doSnowflake/docker/}.}
}
\value{
The full image URI (invisibly).
}
\description{
Generates a Dockerfile from the built-in template, optionally adding
user-specified packages, then builds and pushes the image to a
Snowflake image repository.
}
\details{
Requires Docker to be installed and running locally.
}
\examples{
\dontrun{
conn <- sfr_connect()
uri <- sfr_dosnowflake_build_image(
  conn, "MY_REPO",
  variant = "lean",
  packages = list(cran = c("randomForest", "glmnet"))
)
# uri is now ready to pass to registerDoSnowflake(image_uri = uri)
}

}
