% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect.R
\name{sfr_reinstall}
\alias{sfr_reinstall}
\title{Reinstall and reload snowflakeR (and RSnowflake) from source}
\usage{
sfr_reinstall(
  path = Sys.getenv("SNOWFLAKER_PATH"),
  rsnowflake_path = Sys.getenv("RSNOWFLAKE_PATH")
)
}
\arguments{
\item{path}{Path to the snowflakeR source directory.
Defaults to the \code{SNOWFLAKER_PATH} environment variable.}

\item{rsnowflake_path}{Path to the RSnowflake source directory.
Defaults to the \code{RSNOWFLAKE_PATH} environment variable.
Set to \code{""} to skip RSnowflake reinstallation.}
}
\value{
Invisibly returns \code{TRUE}.
}
\description{
Convenience wrapper for the full reload sequence needed when developing
inside a Workspace Notebook.  Detaches loaded packages, reinstalls from
source, reloads libraries, and refreshes all Python bridge modules.
}
\details{
For most code changes (R function bodies, Python bridge files, NAMESPACE
edits) this is sufficient.  See the package dev standards (Section 13.5)
for cases that require a kernel or container restart instead.
}
