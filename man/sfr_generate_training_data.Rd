% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/features.R
\name{sfr_generate_training_data}
\alias{sfr_generate_training_data}
\title{Generate training data from Feature Views}
\usage{
sfr_generate_training_data(
  fs,
  spine,
  features,
  spine_timestamp_col = NULL,
  spine_label_cols = NULL,
  save_as = NULL,
  exclude_columns = NULL,
  include_feature_view_timestamp_col = FALSE,
  auto_prefix = FALSE,
  join_method = NULL
)
}
\arguments{
\item{fs}{An \code{sfr_feature_store} object.}

\item{spine}{A data.frame, dbplyr lazy table, or SQL string for the
spine (label) data.}

\item{features}{A list of \code{sfr_feature_view} objects (must be registered,
with \code{name} and \code{version} attributes), or a list of lists with \code{name}
and \code{version} elements.}

\item{spine_timestamp_col}{Character. Timestamp column in the spine
for point-in-time joins.}

\item{spine_label_cols}{Character vector. Label columns in the spine.}

\item{save_as}{Character. Optional table name to materialise results.}

\item{exclude_columns}{Character vector or \code{NULL}. Feature or spine column
names to drop from the joined result. Default: \code{NULL} (keep all).}

\item{include_feature_view_timestamp_col}{Logical. If \code{TRUE}, includes each
Feature View's timestamp column in the output. Default: \code{FALSE}.}

\item{auto_prefix}{Logical. If \code{TRUE}, prefixes feature columns with the
Feature View name to avoid name clashes. Default: \code{FALSE}.}

\item{join_method}{Character or \code{NULL}. Join strategy passed to the SDK
(e.g., \code{"AS_OF"}). When \code{NULL}, the SDK chooses a default.}
}
\value{
A data.frame with training data.
}
\description{
Joins spine (label) data with registered Feature Views to produce a
training dataset. Supports point-in-time correct joins when
\code{spine_timestamp_col} is provided.
}
\examples{
\dontrun{
conn <- sfr_connect()
fs <- sfr_feature_store(conn)

training_data <- sfr_generate_training_data(
  fs,
  spine = "SELECT customer_id, label FROM labels_table",
  features = list(
    list(name = "CUSTOMER_FEATURES", version = "v1"),
    list(name = "TRANSACTION_FEATURES", version = "v1")
  ),
  spine_label_cols = "label"
)
}

}
