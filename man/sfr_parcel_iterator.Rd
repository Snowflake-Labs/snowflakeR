% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dosnowflake_queue.R
\name{sfr_parcel_iterator}
\alias{sfr_parcel_iterator}
\title{Create a parcel iterator for bulk I/O in doSnowflake}
\usage{
sfr_parcel_iterator(keys, parcel_size = 50L)
}
\arguments{
\item{keys}{Character vector of partition keys.}

\item{parcel_size}{Integer. Number of keys per parcel (default 50).}
}
\value{
A list of character vectors, one per parcel.
}
\description{
Groups a vector of partition keys (e.g. SKU IDs) into parcels of size
\code{parcel_size}. Each parcel is a character vector that the foreach body
can process as a batch, enabling a single stage read and write per
parcel instead of per-SKU.
}
\examples{
\dontrun{
skus <- paste0("SKU_", 1:200)
parcels <- sfr_parcel_iterator(skus, parcel_size = 50)
# parcels is a list of 4 character vectors, each with 50 SKU IDs

library(foreach)
results <- foreach(parcel = parcels, .combine = rbind) \%dopar\% {
  # Bulk read data for all SKUs in parcel
  # Train models for each
  # Bulk write results
}
}

}
