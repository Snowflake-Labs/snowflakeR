% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{sfr_predict_sql}
\alias{sfr_predict_sql}
\title{Run model inference via SQL (SQL-direct)}
\usage{
sfr_predict_sql(
  conn,
  model_name,
  sql,
  version_name = NULL,
  function_name = "predict",
  parse_json = TRUE
)
}
\arguments{
\item{conn}{An \code{sfr_connection} object from \code{\link[=sfr_connect]{sfr_connect()}}.}

\item{model_name}{Character. Fully-qualified or unqualified model name.}

\item{sql}{Character. SQL query whose results become the model input.}

\item{version_name}{Character. Model version. If \code{NULL}, uses the default
version.}

\item{function_name}{Character. Model function to call. Default: \code{"predict"}.}

\item{parse_json}{Logical. If \code{TRUE} (default), parse the JSON VARIANT
output from \verb{MODEL()!predict()} into a clean data.frame with numeric
columns. Set to \code{FALSE} to return the raw JSON strings.}
}
\value{
A data.frame with prediction results.
}
\description{
Invokes \verb{model!function_name(*)} on the results of a SQL query.
Bypasses the Python bridge entirely for inference, which is useful for
large-scale batch scoring where the data is already in Snowflake.
}
\examples{
\dontrun{
conn <- sfr_connect()
preds <- sfr_predict_sql(
  conn,
  "ML_DB.MODELS.MY_MODEL",
  "SELECT col1, col2 FROM scoring_table",
  version_name = "v1"
)
}

}
