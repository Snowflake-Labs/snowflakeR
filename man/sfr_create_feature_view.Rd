% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/features.R
\name{sfr_create_feature_view}
\alias{sfr_create_feature_view}
\title{Create and register a Feature View in one step}
\usage{
sfr_create_feature_view(
  fs,
  name,
  version,
  entities,
  feature_df,
  refresh_freq = NULL,
  warehouse = NULL,
  timestamp_col = NULL,
  desc = NULL,
  features = NULL,
  feature_granularity = NULL,
  overwrite = FALSE,
  block = TRUE,
  initialize = NULL,
  refresh_mode = NULL,
  cluster_by = NULL,
  online_config = NULL
)
}
\arguments{
\item{fs}{An \code{sfr_feature_store} object.}

\item{name}{Character. Name for the Feature View.}

\item{version}{Character. Version name.}

\item{entities}{An \code{sfr_entity} object or list of \code{sfr_entity} objects.}

\item{feature_df}{A \code{dbplyr} lazy table, SQL string, or table reference
defining the feature transformation logic. Named \code{feature_df} to match
the Python \code{FeatureView} constructor parameter.}

\item{refresh_freq}{Character. Refresh frequency (e.g., \code{"1 hour"},
\code{"30 minutes"}). If \code{NULL}, creates an external (manually maintained)
Feature View.}

\item{warehouse}{Character. Warehouse for refreshing.}

\item{timestamp_col}{Character. Timestamp column for point-in-time lookups.}

\item{desc}{Character. Description.}

\item{features}{List of objects from \code{sfr_feature()} defining aggregation
features. Required when \code{feature_granularity} is set.}

\item{feature_granularity}{Character. Time granularity for tile-based
aggregation (e.g., \code{"1h"}, \code{"1 day"}).}

\item{overwrite}{Logical. If \code{TRUE}, overwrites an existing version.}

\item{block}{Logical. If \code{TRUE} (default), waits until Feature View
materialisation completes before returning.}

\item{initialize}{Character or \code{NULL}. Initial materialisation behaviour for
a managed Feature View (e.g., \code{"ON_CREATE"} to populate on registration).
When \code{NULL}, the Snowflake ML SDK default applies.}

\item{refresh_mode}{Character or \code{NULL}. Refresh strategy for managed views,
such as \code{"INCREMENTAL"} or \code{"FULL"}. When \code{NULL}, the SDK default applies.}

\item{cluster_by}{Character vector or \code{NULL}. Columns to cluster the backing
dynamic table by. When \code{NULL}, no explicit \verb{CLUSTER BY} is set.}

\item{online_config}{An \code{\link[=sfr_online_config]{sfr_online_config()}} object or \code{NULL}. Online
serving configuration; when \code{NULL}, online serving is not configured via
this argument.}
}
\value{
A registered \code{sfr_feature_view} object.
}
\description{
Convenience wrapper that calls \code{\link[=sfr_feature_view]{sfr_feature_view()}} then
\code{\link[=sfr_register_feature_view]{sfr_register_feature_view()}} internally.
}
