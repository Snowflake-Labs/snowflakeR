% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/many_model.R
\name{sfr_log_many_model}
\alias{sfr_log_many_model}
\title{Register a Many-Model Forecast Aggregator}
\usage{
sfr_log_many_model(
  reg,
  model_name,
  version_name,
  model_index,
  partition_columns = "SKU",
  r_packages = "forecast",
  horizon = 12L,
  comment = NULL
)
}
\arguments{
\item{reg}{An \code{sfr_model_registry} or \code{sfr_connection} object.}

\item{model_name}{Character. Model name in the registry.}

\item{version_name}{Character. Version label.}

\item{model_index}{Named list from \code{\link[=sfr_build_model_index]{sfr_build_model_index()}}:
\code{list(partition_key = "stage_path", ...)}.}

\item{partition_columns}{Character vector. Column names forming the
partition key. Default \code{"SKU"}.}

\item{r_packages}{Character vector. R packages loaded at predict time.
Default \code{"forecast"}.}

\item{horizon}{Integer. Forecast horizon. Default 12.}

\item{comment}{Character. Optional comment attached to the version.}
}
\value{
A list with \code{model_name}, \code{version_name}, \code{n_partitions},
\code{registration_time_s}.
}
\description{
Registers a single \code{CustomModel} in Snowflake Model Registry that
dispatches inference to per-partition \code{.rds} files stored on an
internal stage. The model supports both:
\itemize{
\item \code{predict} (TABLE_FUNCTION / \code{@partitioned_api}) for
\code{run_batch()} batch inference.
\item \code{predict_single} (FUNCTION / \code{@inference_api}) for
SPCS service deployment.
}
}
\details{
Registration is O(1) regardless of partition count: ~13 seconds for
2,000 or 10,000 partitions.
}
\examples{
\dontrun{
conn <- sfr_connect()
reg  <- sfr_registry(conn, database = "DEMO_DB", schema = "MODELS")
index <- sfr_build_model_index(conn, run_id = "v2_S_2000sku_4c")

result <- sfr_log_many_model(
  reg,
  model_name = "R_SKU_FORECAST",
  version_name = "V_2k",
  model_index = index,
  partition_columns = "SKU",
  horizon = 12
)
cat("Registered in", result$registration_time_s, "seconds\n")
}
}
