% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/executor.R
\name{sfr_execute_r_script}
\alias{sfr_execute_r_script}
\title{Execute an R Script on SPCS}
\usage{
sfr_execute_r_script(
  conn,
  r_script_stage_path,
  compute_pool,
  image_uri,
  config = list(),
  replicas = 1L,
  entry_fn = "main",
  output_mode = "stage",
  output_format = "rds",
  stage = "DOSNOWFLAKE_STAGE",
  instance_family = "CPU_X64_S",
  eai = "",
  async = TRUE,
  job_name = ""
)
}
\arguments{
\item{conn}{An \code{sfr_connection} object from \code{\link[=sfr_connect]{sfr_connect()}}.}

\item{r_script_stage_path}{Character. Path to the R script on the
mounted stage, e.g. \code{"/stage/scripts/my_script.R"}.}

\item{compute_pool}{Character. SPCS compute pool name.}

\item{image_uri}{Character. Docker image URI for the executor container.}

\item{config}{Named list. Configuration passed to the R entry function
as a list. Written as JSON to the stage.}

\item{replicas}{Integer. Number of parallel workers (default 1).}

\item{entry_fn}{Character. Name of the entry function (default \code{"main"}).}

\item{output_mode}{Character. \code{"stage"}, \code{"table"}, \code{"stdout"}, or
\code{"none"} (default \code{"stage"}).}

\item{output_format}{Character. Output file format for stage results:
\code{"rds"} (default, any R object), \code{"parquet"} (needs arrow pkg),
\code{"csv"}, or \code{"json"}/\code{"ndjson"}. Parquet, CSV, and NDJSON can be
read directly by Snowflake \verb{COPY INTO} or External Tables.}

\item{stage}{Character. Stage to mount in the container (default
\code{"DOSNOWFLAKE_STAGE"}).}

\item{instance_family}{Character. SPCS instance family for resource
sizing (default \code{"CPU_X64_S"}).}

\item{eai}{Character. External Access Integration name for internet
access (default \code{""}).}

\item{async}{Logical. If \code{TRUE} (default), launch asynchronously.
If \code{FALSE}, block until completion.}

\item{job_name}{Character. Custom job service name. Auto-generated
if empty.}
}
\value{
A list with \code{job_id}, \code{job_name}, \code{replicas}, \code{status},
\code{output_path}.
}
\description{
Launches a generic R executor on Snowflake Snowpark Container Services
that loads and runs the specified R script from a mounted stage. The
R script must define an entry function (default \code{main(config)}) that
receives a configuration list.
}
\details{
This avoids Docker image rebuilds -- update the R script on the stage
and re-launch.
}
\examples{
\dontrun{
conn <- sfr_connect()

# Upload script
sfr_upload_r_script(conn, "optimise_sku.R")

# Launch with 4 replicas
job <- sfr_execute_r_script(
  conn,
  r_script_stage_path = "/stage/scripts/optimise_sku.R",
  compute_pool = "MY_POOL",
  image_uri = "/DB/SCHEMA/REPO/r_executor:latest",
  config = list(max_iter = 1000, tolerance = 0.01),
  replicas = 4
)
cat("Job launched:", job$job_name, "\n")

# Poll for completion
status <- sfr_executor_status(conn, job$job_name)
}
}
