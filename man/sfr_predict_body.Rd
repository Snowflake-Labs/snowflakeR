% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{sfr_predict_body}
\alias{sfr_predict_body}
\title{Convert an R function to a predict_body template}
\usage{
sfr_predict_body(fn)
}
\arguments{
\item{fn}{A function with exactly two formals. The first is treated as the
model object; the second as the input data.frame. The function body must
assign its final result to a variable named \code{result}.}
}
\value{
Character scalar. The function body with formals replaced by
\code{{{MODEL}}}, \code{{{INPUT}}}, and local variables suffixed with \code{{{UID}}}.
}
\description{
Takes a function with formals \verb{(model, input)} and converts its body
into the \code{{{MODEL}}}/\code{{{INPUT}}}/\code{{{UID}}} template string expected by
\code{\link[=sfr_log_model]{sfr_log_model()}}.
}
\details{
This lets you write and test your predict logic as a normal R function,
then pass the template to \code{sfr_log_model(predict_body = ...)} without
manually constructing \code{paste()} strings with placeholder syntax.
}
\examples{
\dontrun{
my_predict <- function(model, input) {
  nd     <- as.matrix(input[, c("X1", "X2"), drop = FALSE])
  pred   <- predict(model, newdata = nd)
  result <- data.frame(prediction = as.numeric(pred))
}
body_str <- sfr_predict_body(my_predict)
mv <- sfr_log_model(reg, model = fit, model_name = "MY_MODEL",
                    predict_body = body_str, ...)
}

}
\seealso{
\code{\link[=sfr_log_model]{sfr_log_model()}}, \code{\link[=sfr_predict_local]{sfr_predict_local()}}
}
