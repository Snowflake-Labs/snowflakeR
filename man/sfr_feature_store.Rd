% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/features.R
\name{sfr_feature_store}
\alias{sfr_feature_store}
\title{Connect to the Feature Store}
\usage{
sfr_feature_store(
  conn,
  database = NULL,
  schema = NULL,
  warehouse = NULL,
  create = FALSE,
  default_iceberg_external_volume = NULL
)
}
\arguments{
\item{conn}{An \code{sfr_connection} object from \code{\link[=sfr_connect]{sfr_connect()}}.}

\item{database}{Character. Database for the Feature Store. Defaults to the
connection's current database.}

\item{schema}{Character. Schema for the Feature Store. Defaults to the
connection's current schema.}

\item{warehouse}{Character. Default warehouse for Feature Store compute.
Defaults to the connection's current warehouse.}

\item{create}{Logical. If \code{TRUE}, creates the Feature Store schema and tags
if they don't exist. Default: \code{FALSE}.}

\item{default_iceberg_external_volume}{Character or \code{NULL}. Default external
volume name for Iceberg-backed Feature Store tables. Stored on the returned
object; when \code{NULL}, account or SDK defaults apply.}
}
\value{
An \code{sfr_feature_store} object.
}
\description{
Creates or connects to a Snowflake Feature Store in the specified
database/schema. Returns an \code{sfr_feature_store} object used by other
Feature Store functions.
}
\examples{
\dontrun{
conn <- sfr_connect()
fs <- sfr_feature_store(conn, create = TRUE)
}

}
