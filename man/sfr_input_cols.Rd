% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{sfr_input_cols}
\alias{sfr_input_cols}
\title{Infer input column schema from a data.frame}
\usage{
sfr_input_cols(data, exclude = character(0))
}
\arguments{
\item{data}{A data.frame (e.g. the training data used to fit the model).}

\item{exclude}{Character vector of column names to exclude (case-insensitive).}
}
\value{
A named list mapping column names to type strings
(\code{"double"}, \code{"integer"}, \code{"string"}, \code{"boolean"}).
}
\description{
Builds the \code{input_cols} list expected by \code{\link[=sfr_log_model]{sfr_log_model()}} by inspecting
column types in a data.frame.  Columns listed in \code{exclude} (typically the
target variable and ID columns) are omitted.
}
\examples{
\dontrun{
cols <- sfr_input_cols(training_data,
                       exclude = c("defaulted", "customer_id", "application_id"))
mv <- sfr_log_model(reg, model, "MY_MODEL", input_cols = cols, ...)
}

}
